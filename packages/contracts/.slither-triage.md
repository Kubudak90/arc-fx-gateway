# Slither finding triage

Per Plan 7. Slither runs in CI with `fail-on: medium`. Findings rated medium or higher must either be fixed in code or documented here with a reason. New findings without an entry will fail CI.

Update process:

1. Run `slither packages/contracts --filter-paths 'lib/|test/'` locally.
2. For each finding rated medium+ that we accept, add a row below with: file:line, detector, severity, reason, and the date you reviewed.
3. If a finding's reasoning becomes stale (e.g. surrounding code changed), re-evaluate and either fix or update the row.

Audit scope is `ArcFXGatewayV8.sol`, `ArcFXGatewayV9.sol`, and the libraries they import. V9 is the current production deployment (v0.9, 2026-05-03); V8 is deprecated and kept for reference only. Deprecated contracts (`ArcFXGateway.sol` v0.6/v0.7, `pool/*`, `registry/*`, `OracleAMM.sol`, `PriceGuard.sol`) are pre-audit cleanup candidates, not in scope. Updated L3 (2026-05-06).

## Accepted findings

| File:line | Detector | Severity | Reason | Reviewed |
|---|---|---|---|---|
| _none yet — populate as Slither raises_ | | | | |

## Known false positives

| Pattern | Why it's a false positive | First seen |
|---|---|---|
| _none yet — populate as patterns emerge_ | | |
